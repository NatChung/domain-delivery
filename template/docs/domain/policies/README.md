Rules that decide outcomes: eligibility, windows, formulas, thresholds. A policy
reaching L3 must carry preconditions, postconditions, invariants and invalid
cases, because delivery projects executable checks from them.
