One folder per boundary candidate, created only after several observations
support a durable semantic boundary. Each folder's `INDEX.md` is that context's
`bounded_context` node and its landing page. Folder existence never means the
boundary is confirmed.
