# Domain node authoring contract

The authoring contract is owned by the pinned shared workflow, not by this Hub,
so that Hubs cannot silently fork it. Read it at:

- [`../../.domain-delivery/docs/domain-graph-schema.md`](../../.domain-delivery/docs/domain-graph-schema.md)
- machine-readable mirror:
  [`../../.domain-delivery/kernel/schema/domain-node.schema.json`](../../.domain-delivery/kernel/schema/domain-node.schema.json)

Record Hub-specific authoring conventions below this line — never a second copy
of the contract itself.
